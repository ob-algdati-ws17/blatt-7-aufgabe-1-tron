cmake_minimum_required(VERSION 2.8.2)

project(googletest-download NONE)

include(ExternalProject)
ExternalProject_Add(googletest
        GIT_REPOSITORY    https://github.com/google/googletest.git
        GIT_TAG           master
        SOURCE_DIR        "${CMAKE_BINARY_DIR}/googletest-src"
        BINARY_DIR        "${CMAKE_BINARY_DIR}/googletest-build"
        CONFIGURE_COMMAND ""
        BUILD_COMMAND     ""
        INSTALL_COMMAND   ""
        TEST_COMMAND      ""
        )
project(doxygen-download NONE)

include(ExternalProject)

ExternalProject_Add(doxygen
        GIT_REPOSITORY    https://github.com/doxygen/doxygen.git
        GIT_TAG           master
        SOURCE_DIR        "${CMAKE_BINARY_DIR}/doxygen-src"
        BINARY_DIR        "${CMAKE_BINARY_DIR}/doxygen-build"
        CONFIGURE_COMMAND ""
        BUILD_COMMAND     ""
        INSTALL_COMMAND   ""
        TEST_COMMAND      ""
        )

project(dot-download NONE)

include(ExternalProject)
        
ExternalProject_Add(dot
        GIT_REPOSITORY    https://gitlab.com/graphviz/graphviz.git
        GIT_TAG           master
        SOURCE_DIR        "${CMAKE_BINARY_DIR}/dot-src"
        BINARY_DIR        "${CMAKE_BINARY_DIR}/dot-build"
        CONFIGURE_COMMAND ""
        BUILD_COMMAND     ""
        INSTALL_COMMAND   ""
        TEST_COMMAND      ""
        )
